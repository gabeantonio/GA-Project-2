<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link href="https://fonts.googleapis.com/css2?family=Bungee+Shade&family=Knewave&family=Rubik+Maze&display=swap"
        rel="stylesheet">
    <title>Document</title>
</head>

<body id="show-day">

    <h1 class="day-header">
        Day <%= day %>
    </h1>

    <p class="lead">
        Overview
    </p>

    <div class="plans">

        <% if (events.length) { %>
            <% events.forEach(event=> { %>
                <div id="event">Event:
                    <ul>
                        <li id="list-time">
                            <%= event.time %>
                        </li>

                        <li id="list-activity">
                            <%= event.activity %>
                        </li>

                        <a id="edit" href="/trips/<%= trip._id %>/day/<%= itinerary._id %>/events/<%= event._id %>/edit">
                            Edit Plan
                        </a>
                    </ul>
                </div>

            <% }) %>
        <% } else { %>

            <blockquote id="no-itineraries" class="blockquote">
                <p>No Plans Yet! Would you like to add some?</p>
            </blockquote>

        <% } %>

            <div class="itinerary-nav">
                <div class="itinerary-back">
                    <a href="/trips/<%= trip._id %>"> 
                        <button type="button" class="btn btn-outline-primary">Trip Navigation</button>
                    </a>
                </div>
        
                <div class="itinerary-new">
                    <a href="/trips/<%= trip._id %>/day/<%= itinerary._id %>/events/new">
                        <button type="button" class="btn btn-primary">Add an Event</button>
                    </a>
                </div>
            </div>


    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-OERcA2EqjJCMA+/3y+gxIOqMEjwtxJY7qPCqsdltbNJuaOe923+mo//f6V8Qbsw3" crossorigin="anonymous"></script>
</body>

</html>